trigger AccountTrigger on Account (before insert) {
    system.debug('trigger before insert evetn');

}
//trigger StudentsTrigger on Students (before insert) {
  //system.debug('trigger before insert evetn');